A labelled dropdown matching the Input control. Pass `options` as strings or `{value,label}`.

```jsx
<Select label="Condition" options={['New', 'Used']} />
<Select label="Carrier" options={[{value:'att',label:'AT&T'}, {value:'tmo',label:'T-Mobile'}]} />
```
