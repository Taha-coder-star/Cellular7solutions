A labelled checkbox; graphite fill with white check when selected. Used for filters and the required repair-terms agreement.

```jsx
<Checkbox label="In stock only" defaultChecked />
<Checkbox label="I agree the device may be taken to the store if it can't be fixed on-site." />
```
