A labelled text input — 48px tall, 12px radius, orange focus ring. Supports leading `icon`, `hint`, and `error` text.

```jsx
<Input label="Email" type="email" placeholder="you@example.com" />
<Input label="Phone" icon={<Icon name="phone" size={18} />} error="Required" />
```
