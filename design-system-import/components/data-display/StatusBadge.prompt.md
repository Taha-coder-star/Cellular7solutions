A colored status pill with a leading dot, mapping the backend order/request status enums to semantic colors.

```jsx
<StatusBadge status="in-progress" />
<StatusBadge status="delivered" />
```

Statuses: pending, processing, confirmed, in-progress, shipped, delivered, completed, paid, cancelled, rejected.
