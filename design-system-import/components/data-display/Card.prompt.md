The base surface container — white, 16px radius, thin gray border, soft shadow. Everything sits on a Card.

```jsx
<Card hover>…</Card>
<Card dark padding="var(--space-8)">…</Card>
```

`hover` lifts and deepens the shadow; `dark` switches to a graphite surface for dark sections.
